info:
  name: Login
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/api/login
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
          "login": "john.doe",
          "password": "password123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
