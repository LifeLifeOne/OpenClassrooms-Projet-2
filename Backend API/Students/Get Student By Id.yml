info:
  name: Get Student By Id
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8080/api/students/1
  auth:
    type: bearer
    token: "{{jwt_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
