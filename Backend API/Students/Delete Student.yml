info:
  name: Delete Student
  type: http
  seq: 5

http:
  method: DELETE
  url: http://localhost:8080/api/students/1
  auth:
    type: bearer
    token: "{{jwt_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
