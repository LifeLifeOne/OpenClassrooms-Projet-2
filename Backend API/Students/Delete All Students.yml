info:
  name: Delete All Students
  type: http
  seq: 6

http:
  method: DELETE
  url: http://localhost:8080/api/students
  auth:
    type: bearer
    token: "{{jwt_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
