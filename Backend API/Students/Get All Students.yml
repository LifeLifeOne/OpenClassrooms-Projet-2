info:
  name: Get All Students
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8080/api/students
  auth:
    type: bearer
    token: "{{jwt_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
